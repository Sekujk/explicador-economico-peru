name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: explicador
          POSTGRES_PASSWORD: changeme
          POSTGRES_DB: explicador_economico
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Correr tests (unitarios + integración)
        run: pytest -v
        env:
          DATABASE_URL: postgresql+psycopg2://explicador:changeme@localhost:5432/explicador_economico
